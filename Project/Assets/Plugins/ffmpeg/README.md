# FFmpeg

由于Github无法上传超过100MB的文件，需要用户自行下载并按如下目录结构放置。

## 目录结构

### Windows版本 (`win/`)

#### 1. 可执行文件 (`bin/`)
- `ffmpeg.exe`: 主程序，用于音视频编码、转换和流处理
- `ffplay.exe`: 媒体播放器
- `ffprobe.exe`: 多媒体流分析工具

#### 2. 文档 (`doc/`)

#### 3. 预设配置 (`presets/`)

### macOS版本 (`mac/`)
- `ffmpeg`: macOS平台可执行文件